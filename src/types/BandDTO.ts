export interface BandDTO {
    id: string;
    name: string;
}

export interface BandDetailsDTO {
    albums: string[];
    biography: string;
    genre: string;
    id: string;
    image: string;
    name: string;
    numPlays: number;
}
